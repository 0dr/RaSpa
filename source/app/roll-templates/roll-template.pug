
rolltemplate.sheet-rolltemplate-rolls
    .sheet-template-container
        // - Display a header on the template -
        .sheet-template-header
            | {{#header}} 
            b {{header}}
            | {{/header}}
        // - Display the dice roll in the template -
        .sheet-template-body
            | {{#dice}}
            .sheet-template-row {{dice}}
            | {{/dice}}
            // - Display a description. This is great for text included with a roll -
            | {{#desc}}
            .sheet-template-row
                span.sheet-template-desc {{desc}}
            | {{/desc}}



rolltemplate.sheet-rolltemplate-test 
    .sheet-template-container
        h1 {{name}}
        .sheet-results
            h4 Result = {{roll1}}
            h4 Custom Result = {{computed::roll1}}

//- - const attributes = ["Ausdauer", "Gewandtheit", "Muskelkraft", "Rationalität", "Kreativität", "Willenskraft"]
rolltemplate.sheet-rolltemplate-attributes
    .sheet-template-container
        .sheet-template-header
            | {{#header}} 
            b {{header}}
            | {{/header}}
        .sheet-template-body
            | {{#aud}}
            .sheet-template-row {{aud}}
            | {{/aud}}
            | {{#geh}}
            .sheet-template-row {{geh}}
            | {{/geh}}
            | {{#muk}}
            .sheet-template-row {{muk}}
            | {{/muk}}
            | {{#rat}}
            .sheet-template-row {{rat}}
            | {{/rat}}
            | {{#krt}}
            .sheet-template-row {{krt}}
            | {{/krt}}
            | {{#wik}}
            .sheet-template-row {{wik}}
            | {{/wik}}
            | {{#qual}}
            .sheet-template-row
                span.sheet-template-desc {{qual}}
            | {{/qual}}
